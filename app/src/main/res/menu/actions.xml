<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_backup"
        android:icon="@drawable/ic_action_backup"
        android:title="backup"
        android:iconTint="@color/textWhite"
        android:iconTintMode="add"
        android:tooltipText="Backup your tags to google drive"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_logout"
        android:icon="@drawable/ic_action_logout"
        android:iconTint="@color/textWhite"
        android:iconTintMode="add"
        android:title="backup"
        android:tooltipText="Logout of the app"
        app:showAsAction="ifRoom" />

</menu>
